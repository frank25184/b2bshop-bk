"use strict";
const express = require('express'),
      router = express.Router(),
      auth = require('../middlewares/auth');

      module.exports = function (app){
        // app.get('/site/:site', function(req,res){
        //     let site = req.params.site;
        //     res.redirect(301, '/review/'+site);
        // });

      };

